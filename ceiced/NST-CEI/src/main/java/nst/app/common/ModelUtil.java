package nst.app.common;

public class ModelUtil {

  /*
  public <M extends ChildModel> M myChild(Long childId, Collection<M> allChild) {
    if (childId != null) {
      Optional<M> first = allChild.stream().filter(e -> childId.equals(e.getId())).findFirst();
      if (first.isPresent()) {
        return first.get();
      }
    }
  }
*/
}